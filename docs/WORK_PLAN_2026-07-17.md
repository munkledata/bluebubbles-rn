# Work plan — 2026-07-17

> **Execution status (same day, evening pass):** items 1, 3, 4, 6, 7, 9 and the mark-unread +
> reactions bugs in item 5 are ✅ DONE (code + tests, uncommitted; full gate green —
> typecheck clean, 256 suites / 1,798 tests, lint 0 errors). Multipart (item 5c) is
> confirmed SERVER-GATED (exact server change identified below). Item 2 (the on-device
> verification pass) is the user's session. Item 10 dropped by user decision.

Source: the full-docs-vs-code audit of 2026-07-17 (all 24 markdown docs cross-checked against
`src/`, `app/`, `test/`, and the server repo where reachable). Every item below was **verified
missing or unfinished in code** — this list is trustworthy in a way the older checklists no
longer are. Items are ordered by priority; each has scope, the files involved, and how to
verify it's done.

Legend: **P0** do next · **P1** soon · **P2** when convenient · **P3** blocked on
credentials/hardware or explicitly backlog.

---

## P0 — do next

### 1. ✅ DONE — RCS new-chat toggle (CORRECTED 2026-07-17: send already works)
Re-verification showed RCS send to an EXISTING chat works end-to-end (server routes by the
`RCS;-;` guid, ack reconcile handles the tempGuid echo, outgoing bubbles teal). The only gap:

- What: add "RCS" to the new-chat service toggle (gated on `useRcsEnabled`), passing
  `service:'RCS'` through the existing `createChat` — the server's `new-chat` RCS branch
  (sidecar get-or-create) already handles it.
- Where: `app/(app)/new-chat.tsx` (toggle is `'iMessage' | 'SMS'` today).
- Verify: component/unit test that RCS is offered when the capability flag is on and the
  created chat carries `service:'RCS'`; then a live new-RCS-chat on a paired account.
- Size: small.

### 2. On-device verification pass
Nothing has been ticked since the SDK 57 native rebuild. All the risky native surfaces
(notifications, killed-app FCM decrypt, app-lock cold boot, share intent, FaceTime
full-screen intent) are code-complete but unproven on this build.

- What: clean rebuild (`rm -rf android && expo run:android`), then run every box in
  `docs/DEVICE_VERIFICATION_CHECKLIST.md` (~33 checks) and record pass/fail in that doc.
  Fold in the 4 `docs/SPIKES.md` gates where practical (60fps @ 100k messages, headless FCM
  write, header-auth e2e, libsodium interop — crypto/rekey/FTS5 already partially proven).
- Verify: the checklist itself IS the verification; tick boxes with dates.
- Size: one focused device session (plus fixes for whatever it surfaces).

### 3. ✅ DONE — Turn off Android `allowBackup`
Open security item (SEC-6). The generated manifest still has `android:allowBackup="true"`,
which lets `adb backup`/device-transfer copy app data.

- What: set it false via `expo-build-properties` (or a config plugin) in `app.config.ts` so
  the generated manifest carries `allowBackup="false"`.
- Verify: after prebuild, grep `android/app/src/main/AndroidManifest.xml` for
  `allowBackup="false"`.
- Size: small (config change + rebuild).

---

## P1 — soon

### 4. ✅ DONE — Socket URL failover (`refreshUrl` → resolver)
_(Shipped as `src/services/realtime/serverUrlResolver.ts`, wired in `realtimeControl.ts`;
session-store origin is the first source, Firebase-RTDB slots in later as another source.)_
The hook exists (`src/services/realtime/socketService.ts:27`) but nothing is plugged into
it — when the tunnel URL rotates while the socket is down, escalation reconnects to the
same dead origin forever.

- What: build a small `ServerUrlResolver` and wire it at the composition root
  (`src/services/realtimeControl.ts`). First implementation can re-read the stored server
  URL / retry `new-server` state; the Firebase-RTDB fetch of the fresh tunnel URL is the
  full fix but is credential-gated (see P3 #12) — design the resolver so Firebase slots in
  later.
- Verify: unit test — resolver returns a new URL, socket reconnects to it.
- Size: small-medium.

### 5. Verified small bugs (each independently shippable)
- ✅ **Reactions on attachment-only messages** — was ALREADY fixed in `b217bea` (2026-07-16,
  `attsReactionAnchor` in `MessageBubble`); gallery-path regression test added 2026-07-17.
- ✅ **Mark-unread now syncs to the Mac** — `markChatUnread` endpoint + best-effort service
  call after the local write (RCS + private-API-off skipped), shipped 2026-07-17.
- ⛔ **Text + attachment as one bubble — SERVER-GATED** (verified 2026-07-17): no server
  route accepts text+attachment together. Exact server change: add `text` to the
  `send-attachment` zod input (`actionOperations.ts`), thread through
  `MessageSender.sendAttachmentFile`'s `compact()` whitelist + the streamed upload route;
  whether the helper's handler consumes it needs an empirical test on the Mac.

### 6. ✅ DONE — Attachment forwarding
Shipped 2026-07-17: downloaded attachments (any type) forward into new-chat's staging tray
via `src/features/conversations/forwardParams.ts` (JSON string param, validated on receipt);
undownloaded-only messages get a "download first" notice, never an auto-download.
Still worth a device check: forward an image end-to-end.

---

## P2 — when convenient

### 7. ✅ DONE — Scheduled-message recurrence
Shipped 2026-07-17: nullable `recurrence` column (migration `0024`), pure `nextOccurrence`
helper (month-end clamp, catch-up skips stale slots), atomic re-arm guarded on the claim
(`rearmScheduled`), picker in the composer schedule sheet + edit screen, "Repeats …" labels
on scheduled rows. Recurring rows are LOCAL-ONLY (the server has no repeat concept).
Zero recurrence modeling exists (schema, repo, service, UI all one-shot).
- What: `recurrence` column(s) + migration (append-only, mirror into schema/model/helpers
  per the migration checklist in AGENTS.md), expand `runDueScheduled` to re-arm recurring
  rows after send, picker UI in the schedule sheet.
- Verify: node tests around the ticker (claim → send → re-arm), device check for the alarm.
- Size: medium.

### 8. Multi-alias send-from picker — SERVER-GATED (verified 2026-07-17)
The server's `send-text` input carries no per-send alias/from field, so this needs
server+helper work first. Moved to blocked; app-side picker exists in `account.tsx`.

### 9. ✅ DONE — QR pairing display
Shipped 2026-07-17: "Show Pairing QR" in server-management, reveal-gated with a security
warning (payload carries the password), re-hidden on focus loss; pure-JS `qrcode` matrix
rendered as Views (`src/ui/primitives/QrCode.tsx` / `PairingQr.tsx`); payload round-trips
through the app's own scanner format (`buildSetupQr` ↔ `parseSetupQr`).

### 10. ~~Server update check/install~~ — DROPPED by user decision (2026-07-17)

### 11. Find My items (AirTags)
RN side needs the `FindMyItem` model + `normalizeItem` + items endpoint + an Items section
(plan: `docs/FINDMY_DECRYPTION_PLAN.md` Phase 4 item 16) — but it's **blocked on the
server-side decrypt phases (1-2)** in the server repo. Do the server phases first.
- Size: medium, cross-repo.

---

## P3 — blocked on credentials/hardware, or backlog by choice

12. **Server-side Firebase push of rotated tunnel URL** — needs the Firebase project
    credentials wired server-side; unblocks the full version of #4.
13. **EAS build/submit** — needs Expo account login + credentials (release gate).
14. **Sentry** — needs a DSN; init + `componentDidCatch` wiring + PII scrubber are planned
    in RELEASE_CHECKLIST §9.2.
15. **Tasker/automation native receiver** — needs a native rebuild with the exported
    receiver; JS intent layer exists.
16. **Transcript background SEND** — receive is shipped; send is SIP-gated helper work in
    the server/helper repos (poster authoring proven; helper handler + live test open).
17. **Cert-pin activation** — supply the real SPKI hash + rebuild (code path is
    no-op-without-config by design).
18. **Parity backlog** — ~54 medium + ~138 low gaps in
    `docs/OLD_APP_PARITY_AUDIT_2026-07-15.md` and the 66-candidate list in
    `docs/GITHUB_ISSUES_FEATURE_AUDIT_2026-07-15.md`. Top picks when mining it: contact
    card / add-to-contacts, @mention autocomplete, address validation + E.164, in-app PDF
    viewer, unified SMS inbox (#21 — the single biggest community ask).
19. **Light/Dark/System theme mode** — deliberately deferred by user choice; leave until
    asked.

---

## Standing rules for this plan

- Before claiming any item done: `npm run typecheck && npm test`, plus the item's own
  verify step above. Device-surface items additionally need their
  `DEVICE_VERIFICATION_CHECKLIST.md` box ticked on hardware.
- When an item closes, update THIS file and the doc that tracks the feature — the
  2026-07-17 audit showed the docs rot fast when that step is skipped.
